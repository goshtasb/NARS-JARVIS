# Vendored upstream clones — re-clone from their own repos; not this project's source.
/OpenNARS-for-Applications/
/NARS-GPT/
/OmniGlass/

# Python
__pycache__/
*.py[cod]

# Local runtime artifacts
*.db
*.log

# runtime state (local L2 db)
jarvis.db
src/jarvis.db

# local LLM weights (multi-GB, not version-controlled)
models/

# compiled macOS sensor helpers (built on demand)
src/sentinel/.sensor.bin
src/sentinel/.fswatch.bin

# compiled menu-bar app bundle (built on demand via ui/build.sh)
src/ui/build/

# macOS junk
.DS_Store

# local telemetry/output artifacts
*.csv
nohup.out

# launchd plist staging output (generated by setup-launchd.sh)
/dist/

# SQLite WAL sidecars (runtime artifacts of journal_mode=WAL, ADR-052)
jarvis.db-shm
jarvis.db-wal
src/jarvis.db-shm
src/jarvis.db-wal
